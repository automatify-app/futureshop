<div class="popup-content popup-password">
   <div id="Login" data-lightbox-content class="popup-inner">
      <h4>{{ 'general.password_page.login_form_heading' | t }}</h4>
      {% form 'storefront_password'  %}
        {{ form.errors | default_errors }}
        <label for="password">{{ 'general.password_page.login_form_password_label' | t }}</label>
        <input type="password"
          name="password"
          id="Password"
          placeholder="{{ 'general.password_page.login_form_password_placeholder' | t }}">
        <button type="submit" name="commit" class="button secondary">
          {{ 'general.password_page.login_form_submit' | t }}
        </button>
      {% endform %}
      <p>{{ 'general.password_page.admin_link_html' | t }}</p>
    </div>
</div>